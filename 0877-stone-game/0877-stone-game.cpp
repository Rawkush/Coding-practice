class Solution {
public:
    // bool helper(vector<int>& piles, int i, int j, int sum, int turn) {
    //     if(i>j) return false;
    //     if(sum<=0) return true;
    //     int mx =0;
    //     if()

    // }

    bool stoneGame(vector<int>& piles) {
        return true;
    }
};